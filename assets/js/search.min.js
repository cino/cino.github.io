const searchButton = document.querySelectorAll('.toggle-search');
const searchDiv = document.querySelector('.site-search');

const toggleSearch = () => {
    searchDiv.classList.toggle('site-search-hidden');
}

searchButton.forEach(button => button.addEventListener('click', toggleSearch, false));
